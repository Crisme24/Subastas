{% extends 'base.html.twig' %}

{% block title %}{{parent()}} - subastas{% endblock %}

{% block body %}

<div class="">
    <div class="container h-100 ">
        <div class="row h-100 justify-content-center align-items-center ">
            <div class="col-5 bg-white rounded my-3 p-5">
            {% if edit is defined %}
                <h1 class="h3 mb-3 font-weight-normal text-center">Editar subasta</h1>
            {% else %}
                 <h1 class="h3 mb-3 font-weight-normal text-center">Crear subasta</h1>
            {% endif %}
                {{form(form)}}
            </div>
        </div>
    </div>
{% endblock %}
